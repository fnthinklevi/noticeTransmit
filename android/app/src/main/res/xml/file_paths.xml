<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 应用内更新：已通过签名校验的暂存副本所在目录（filesDir/update） -->
    <files-path name="update_apk" path="update/" />
    <!-- 导出/日志等临时文件（cacheDir） -->
    <cache-path name="cache" path="." />
</paths>
